# Core
watchdog>=3.0.0
Pillow>=10.0.0
pystray>=0.19.0

# Image dedup (optional — system degrades gracefully without these)
opencv-python-headless>=4.5.0
numpy>=1.19.0

# Python 3.10 only (3.11+ has tomllib in stdlib)
# tomli>=2.0.0
